import 'package:flutter/material.dart';

class SplitEnds extends StatelessWidget {
  const SplitEnds({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
