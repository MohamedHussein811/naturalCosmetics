import 'package:flutter/material.dart';

class HairLoss extends StatelessWidget {
  const HairLoss({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
